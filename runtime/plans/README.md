# plans

本目录用于多活跃长任务的计划升级。  
它是运行对象，不属于 `docs/` 说明层。  
只有当 `runtime/PLANS.md` 不再足够时，才启用 `active/` 与 `archive/`。

## 什么时候升级

- 出现多个活跃长任务
- 单一 `runtime/PLANS.md` 已无法清楚承接不同状态、不同验证路径或不同下一步

## 使用原则

- `runtime/PLANS.md` 保留总览与索引
- 活跃计划进入 `active/`
- 已完成或已关闭计划进入 `archive/`
- 每个计划文件仍需保持 `PLANS` 固定字段完整
