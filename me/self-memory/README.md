# self-memory

本目录是 `me/` 下的维护者自我复盘可选包。  
它只给维护者阅读，不进入 AI 侧默认正式真相源。

## 当前对象

- `extracts/`：单个 raw 的结构化提取
- `batches/`：按月滚动批次
- `reports/`：`micro / periodic` 报告
- `profile/`：长期协作画像
- `archive/`：过期报告或旧批次的辅助归档

## 边界

- 不保存长期 raw 原文
- 不替代 `runtime/` 中的正式对象
- 只有维护者明确确认后，才允许把结论手动转成 AI 侧 proposal
