# configs

本目录用于项目级配置物料。  
默认只保留和 `V3` 结构相关的最小入口，不把真实密钥或环境专属值直接写进仓库。

## 当前范围

- `.codex/config.toml`：Codex 项目级配置入口
- `models/`：`AI Pack` 启用后使用的模型配置目录

## 使用边界

- 没启用 `AI Pack` 时，不必在 `models/` 中铺更多文件
- 真实密钥和账号信息不进入本目录
- 配置改动如果影响 workflow、skill 或验证脚本，要同步更新根级入口说明
