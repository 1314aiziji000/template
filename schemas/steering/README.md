# steering schemas

本目录放 `V3` 进化系统的机器契约。

## 当前对象

- `error-record.schema.json`
- `evolution-proposal.schema.json`
- `evolution-memory.schema.json`

## 边界

- 这里不写说明文，不替代 `docs/protocols/steering-lite.md`
- 契约只约束结构，不替代 owner 判断
